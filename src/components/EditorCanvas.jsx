import React from 'react';
import './styles/EditorCanvas.css';

const EditorCanvas = () => {
    return (
        <div className="editor-canvas">
            {/* React Flow replaces this placeholder in Phase 4 */}
            <p className="editor-canvas__placeholder">Canvas goes here</p>
        </div>
    );
};

export default EditorCanvas;
